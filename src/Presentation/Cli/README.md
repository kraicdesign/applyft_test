# CLI presentation

Artisan commands and console route registrations belong in this directory. A CLI command parses console input, creates an Application command or query, invokes its handler, and formats output. It must not contain business rules or call Infrastructure adapters directly.
